{
  "name": "@alterford/gateway",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "main": "dist/cli.js",
  "scripts": {
    "build": "tsc -p tsconfig.json --emitDeclarationOnly false",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/cli.js"
  },
  "dependencies": {
    "@alterford/sdk": "workspace:*",
    "@biconomy/abstractjs": "^1.2.5",
    "@gelatocloud/gasless": "0.0.12",
    "@fleek-platform/sdk": "3.7.2",
    "json-bigint": "^1.0.0",
    "viem": "^2.45.0"
  },
  "devDependencies": {
    "@types/json-bigint": "^1.0.4"
  }
}
